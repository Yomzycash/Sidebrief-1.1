import HeadText from "./HeadText";
import TextsWithLink from "./TextWithLinks";

export { HeadText, TextsWithLink };
export { TextWithArrow } from "./TextWithArrow"; // Can be used in links and buttons, it's just text so the parent still has to perform it's function
