import styled from 'styled-components';

export const NavWrapper = styled.div({
    alignItems: 'center',
    display: 'flex',  
    flexDirection: 'row',
    padding: '0px 100px',
    height: 100,
});

export const Image = styled.img({
    
});