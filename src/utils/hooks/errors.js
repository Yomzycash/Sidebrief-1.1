// const { default: toast } = require("react-hot-toast");

export const handleErrorHook = (error) => {
  // console.log(error);
  // if (error?.status === "FETCH_ERROR") {
  //   toast.error("Please check your internet connection");
  // } else {
  //   toast.error(error?.data.message);
  // }
};
